# Copie este archivo como ".env" y complete la clave real.
# El archivo ".env" está en .gitignore: nunca se versiona.

# Clave de API para el LLM en la nube (Blablador, Jülich Research Centre).
# Solo es necesaria para las etapas 04 y 05 ejecutadas con el flag --cloud.
SIES_BLABLADOR_API_KEY=reemplace_por_su_clave
